package com.immediatlyApp.repositories;

import com.immediatlyApp.models.entity.UserDidServiceType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserDidServiceTypeRepository extends JpaRepository<UserDidServiceType,Long> {
}
