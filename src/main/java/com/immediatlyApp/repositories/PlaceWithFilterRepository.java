package com.immediatlyApp.repositories;

import com.immediatlyApp.models.entity.PlaceWithFilter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlaceWithFilterRepository extends JpaRepository<PlaceWithFilter,Long> {
}
